# Context
<!-- Auto-maintained by the stop hook. Edit manually if needed, but keep it short. -->

**Current focus:** Nothing active — feature-request session concluded; shipped work needs real usage before anything further is built
**Last session:** Shipped 6 backlog items (stub-first workflow, Mermaid `/log` diagrams, hardened debugging Verify step, `guard-naming.sh` regex broadening, `.claude/claudify` commit-SHA pinning, `/prep` secrets scan); blind-agent testing caught and fixed a real gap (secrets scan missed untracked-file content); reworded the stub-first rule to be language-agnostic (ADR-14); backlogged BDD/system-level scenario stubs and a manual plan-mode-style toggle hypothesis for both that and the default-to-confirm rule
**Blocking:** Runner fix (ADR-7) still unverified on Linux/WSL — `bug-report-claudify-exit.md` stays local until then
**Next action:** Use `people`/`transcripts` and the new stub-first workflow in a real project first; then resume the add-ons roster or pick something else from the backlog
<!-- wrapped: 2026-08-18 -->
