name: Python TUI
description: Claude Code setup for Python TUI projects using Textual

# To apply this blueprint, tell Claude Code:
#   "Read and apply claudify/blueprints/python-tui.yml to this project."
#
# Rules:
#   - All files are skipped if they already exist (never overwrites)
#   - executable: true  → chmod +x after creating
#   - setup steps run after all files are installed

files:

  # --- Project documents ---
  # Uses Python TUI-specific CLAUDE.md and ARCHITECTURE.md;
  # shares generic CONTEXT.md, PLAN.md, ROADMAP.md

  - src: files/docs/CLAUDE-python-tui.md
    dest: CLAUDE.md

  - src: files/docs/CONTEXT.md
    dest: CONTEXT.md

  - src: files/docs/PLAN.md
    dest: PLAN.md

  - src: files/docs/ARCHITECTURE-python-tui.md
    dest: ARCHITECTURE.md

  - src: files/docs/ROADMAP.md
    dest: ROADMAP.md

  # --- Hooks (shared with generic) ---

  - src: files/hooks/session-wrap.sh
    dest: .claude/hooks/stop/session-wrap.sh
    executable: true

  - src: files/hooks/guard-naming.sh
    dest: .claude/hooks/pre-tool-use/guard-naming.sh
    executable: true

  # --- Commands (shared with generic) ---

  - src: files/commands/continue.md
    dest: .claude/commands/continue.md

  - src: files/commands/save.md
    dest: .claude/commands/save.md

  - src: files/commands/log.md
    dest: .claude/commands/log.md

  - src: files/commands/prep.md
    dest: .claude/commands/prep.md


setup:
  - mkdir: .claude/archive
  - gitignore: .claude/archive/

next_steps:
  - Fill in CLAUDE.md — entry point, screen/widget paths, package manager
  - Fill in ARCHITECTURE.md ## Quick Reference — entry point, key paths, constraints
  - Fill in ARCHITECTURE.md ## Detail — app structure, screen map, state model
  - Fill in CONTEXT.md — current focus and next action
